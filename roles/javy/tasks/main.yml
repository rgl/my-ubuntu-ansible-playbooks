- name: Install javy
  args:
    executable: /bin/bash
  ansible.builtin.shell: "{{ lookup('file', 'install-javy.sh') }}"
  environment:
    JAVY_VERSION: "{{ javy_version }}"
  register: javy_result
  changed_when: "'ANSIBLE CHANGED NO' not in javy_result.stdout_lines"
