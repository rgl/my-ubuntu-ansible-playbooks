- name: Install solo2
  args:
    executable: /bin/bash
  ansible.builtin.shell: "{{ lookup('file', 'install-solo2.sh') }}"
  environment:
    SOLO2_VERSION: "{{ solo2_version }}"
  register: solo2_result
  changed_when: "'ANSIBLE CHANGED NO' not in solo2_result.stdout_lines"
